# Generated sample data

Run `agentic-data-generate --output data/sample` to create the five deterministic Parquet
datasets and their metadata sidecars. Parquet output is intentionally excluded from version
control so the repository stays small and the generator remains the source of truth.
